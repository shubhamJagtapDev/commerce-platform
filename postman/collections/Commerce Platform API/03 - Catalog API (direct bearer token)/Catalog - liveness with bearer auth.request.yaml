$kind: http-request
url: "{{catalogBaseUrl}}/actuator/health/liveness"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('returns HTTP 200', function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 1000
